# Emscripten-specific addons configuration
# Note: Some addons from the native version won't work in the browser

# Required for Emscripten support
ofxEmscripten

# GUI - should work in browser
ofxGui

# OSC - may need websocket adapter for browser
# ofxOsc

# Network addons - typically don't work in browser without modifications
# ofxMQTT - MQTT over websockets might work but needs configuration
# ofxNDI - Native video streaming, won't work in browser

# Testing
# ofxUnitTests
